{{#*inline "page"}}

<div class="header">
  <h1>^o^ laenorweb2</h1>
  <h2>subtitle</h2>
</div>


<div class="pure-g">
  {{#each taggroups as |tg| ~}}
    <div class="pure-u-1"><div class="box">
      <h3>{{ tg.0.name }}</h3>
    </div></div>
    {{#each tg.1 as |tag| ~}}
      <div class="pure-u-1 pure-u-md-1-3 pure-u-lg-1-5">
        <a href="">
          <div class="box area">
            <div class="maploc"><pre>{{ tag.map }}</pre></div>
            <div class="name">{{ tag.name }}</div>
          </div>
        </a>
      </div>
    {{/each~}}
  {{/each~}}
</div>

{{/inline}}
{{~> (parent)~}}
